version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - echo "Installing dependencies..."
      - cd backend
      - npm install
run:
  runtime-version: nodejs18
  command: cd backend && node test-server.js
  network:
    port: 8080
    env: PORT
  env:
    - name: NODE_ENV
      value: production
